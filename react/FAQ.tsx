import React, { useEffect, FunctionComponent, Fragment } from 'react'

const FAQ: FunctionComponent = () => {
  useEffect(() => {
    window.location.assign('https://vtex.io/docs')
  })

  return <Fragment></Fragment>
}

export default FAQ
